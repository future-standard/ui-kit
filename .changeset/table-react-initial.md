---
'@future-standard-ui/table': minor
'@future-standard-ui/theme': minor
---

Initial React table: schema-driven `DataTable`, composable primitives (`Table.Root` … `Table.Cell`),
`useTable` binding to `table-core`, cell renderer registry, loading/empty/error slots, sortable
headers, single/multiple selection with select-all, expandable drawers, header groups, grouped rows,
zebra, density, sticky header, row tone/state hints, and container-query column visibility.
Theme gains the `--table-*` colour aliases.
