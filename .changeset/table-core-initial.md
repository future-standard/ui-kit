---
'@future-standard-ui/table-core': minor
---

Initial framework-free table engine: JSON schema with validation, controlled/uncontrolled state
slices (sorting, selection, expansion, column visibility, pinning, collapsed groups), row model with
dot-path accessors and opt-in client sorting, header groups, adjacent-row grouping, and the DOM
contract every renderer emits.
